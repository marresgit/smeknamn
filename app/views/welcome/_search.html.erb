<% if @nickname.blank? %>
  <%= @nickname.each do |nicknames| %>
    <%= nicknames.name %>
    <%= nicknames.nick %>
  <% end %>
<% else %>
  <p>There are no posts containing the term(s) <%= params[:search] %>.</p>
<% end %>
